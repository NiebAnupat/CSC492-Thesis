{
  "name": "CSC492-Thesis",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "setup:project": "pnpm install && concurrently \"(cd frontend && pnpm install)\" \"(cd server && pnpm install)\"",
    "dev": "concurrently \"(cd frontend && pnpm dev)\" \"(cd server && pnpm start:dev)\"",
    "postgres:migration" : "node migration.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "dotenv": "^16.4.5",
    "pg": "^8.12.0"
  }
}
